# Telegram AI Bot с Google Gemini

Профессиональный Telegram бот с искусственным интеллектом на базе Google Gemini API, работающий через VPN прокси для обхода географических ограничений.

## 🚀 Возможности

- **🤖 Google Gemini AI** - мощная языковая модель для общения
- **🔄 VPN прокси поддержка** - обход географических ограничений
- **💬 Контекстные диалоги** - запоминает историю разговора
- **📊 Аналитика и статистика** - подробная статистика использования
- **🗄️ SQLite база данных** - надежное хранение данных
- **⚡ Асинхронная архитектура** - высокая производительность
- **🛡️ Безопасность** - фильтрация контента и защита
- **🎛️ Гибкая конфигурация** - настройка через переменные окружения

## 📋 Требования

- Python 3.8+
- Токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))
- API ключ Google Gemini (получить в [Google AI Studio](https://aistudio.google.com/))
- VPN/V2Ray прокси (для доступа к Gemini из РФ)

## 🛠️ Установка и настройка

### 1. Клонирование и установка зависимостей

```bash
# Активация виртуального окружения
venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка V2Ray прокси

Убедитесь, что у вас запущен V2Ray с конфигурацией:
- HTTP прокси: `127.0.0.1:10809`
- SOCKS прокси: `127.0.0.1:10808`

### 3. Настройка бота

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   copy .env.example .env
   ```

2. Отредактируйте `.env` файл:
   ```env
   # Токен вашего Telegram бота
   BOT_TOKEN=your_actual_bot_token_here
   
   # API ключ Google Gemini (уже вставлен)
   GEMINI_API_KEY=AIzaSyDorWMeWBe_D-Kq3I8dRg9s3eFWq6vcOBM
   
   # ID администратора (ваш Telegram ID)
   ADMIN_ID=123456789
   ```

### 4. Запуск бота

```bash
python bot.py
```

## 📁 Структура проекта

```
dark_adventure/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация и настройки
├── gemini_client.py    # Клиент Google Gemini API с прокси
├── database.py         # Управление базой данных SQLite
├── session_manager.py  # Управление сессиями и диалогами
├── requirements.txt    # Зависимости Python
├── .env.example       # Пример конфигурации
├── .env               # Ваша конфигурация (создается вами)
├── bot.db             # База данных SQLite (создается автоматически)
└── README.md          # Этот файл
```

## 🤖 Команды бота

### Основные команды:
- `/start` - Приветствие и регистрация
- `/help` - Помощь и информация
- `/clear` - Очистить историю диалога
- `/info` - Информация о боте и статистика

### Административные команды:
- `/stats` - Детальная статистика (только для админа)

## 💾 Архитектура

### Компоненты системы:

1. **bot.py** - основной класс бота с обработчиками
2. **gemini_client.py** - асинхронный клиент для Gemini API
3. **database.py** - менеджер SQLite базы данных
4. **session_manager.py** - управление сессиями пользователей
5. **config.py** - централизованная конфигурация

### База данных:

**Таблицы:**
- `users` - информация о пользователях
- `messages` - история сообщений
- `conversations` - контекст диалогов
- `response_cache` - кэш ответов Gemini
- `statistics` - ежедневная статистика

## 🔧 Конфигурация

### Основные параметры `.env`:

```env
# Токен бота и API ключи
BOT_TOKEN=your_bot_token
GEMINI_API_KEY=your_gemini_api_key

# Настройки прокси
HTTP_PROXY=http://127.0.0.1:10809
SOCKS_PROXY=socks5://127.0.0.1:10808
PROXY_TYPE=http

# Администратор
ADMIN_ID=123456789

# Настройки Gemini
GEMINI_TEMPERATURE=0.7
GEMINI_MAX_TOKENS=2048
SYSTEM_PROMPT=Ты — дружелюбный ассистент...
```

## � Особенности работы

### Работа через VPN прокси:
Бот автоматически использует настроенный прокси для доступа к Google Gemini API, обходя географические ограничения.

### Контекстные диалоги:
Бот запоминает последние 20 сообщений в диалоге, обеспечивая контекстное общение.

### Кэширование:
Ответы на повторные вопросы кэшируются на 1 час для ускорения работы.

### Безопасность:
- Фильтрация запрещенного контента
- Ограничение длины сообщений
- Защита от спама

## � Мониторинг

### Логирование:
Бот ведет детальное логирование всех операций:
- Запросы к Gemini API
- Ошибки и предупреждения
- Статистика использования

### Статистика:
- Количество пользователей и сообщений
- Время ответа API
- Активные сессии
- Использование кэша

## 🔒 Безопасность

- Токены и ключи хранятся в переменных окружения
- Параметризованные SQL-запросы
- Фильтрация входящих сообщений
- Ограничения на размер сообщений

## 🚀 Производительность

- Асинхронная обработка запросов
- Кэширование ответов
- Оптимизированные запросы к БД
- Фоновая очистка устаревших данных

## �️ Разработка

### Добавление новых команд:

```python
async def new_command(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("Новая команда!")

# В _setup_handlers():
self.app.add_handler(CommandHandler("new", self.new_command))
```

### Изменение системного промпта:

Отредактируйте `SYSTEM_PROMPT` в `.env` файле или в `config.py`.

## 🐛 Устранение проблем

### Ошибки подключения к Gemini:
1. Проверьте работу V2Ray прокси
2. Убедитесь что API ключ действителен
3. Проверьте настройки прокси в `.env`

### Ошибки базы данных:
1. Проверьте права доступа к файлу `bot.db`
2. Убедитесь что установлена `aiosqlite`

### Медленные ответы:
1. Проверьте скорость интернет-соединения
2. Увеличьте `REQUEST_TIMEOUT` в конфигурации

## 📈 Масштабирование

Для расширения функционала:
- Добавьте новые команды в `bot.py`
- Расширьте схему БД в `database.py`
- Настройте вебхук вместо polling
- Добавьте поддержку медиа файлов

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь в правильности конфигурации
3. Проверьте работу VPN прокси

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

**Создано с ❤️ для мамы!** 🌸
